# Brewfile — dev environment for Android (Java) + iOS (Swift) project
# Usage: brew bundle --file=Brewfile

# Core tools
brew "git"
brew "gh"

# Gradle build tool
brew "gradle"

# JDK 17 (Eclipse Temurin)
cask "temurin@17"

# Android command-line tools (includes sdkmanager)
cask "android-commandlinetools"
